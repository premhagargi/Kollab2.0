
// src/components/layout/AppSidebar.tsx
// This component is no longer used and its functionality has been moved to AppHeader.
// Keeping the file for now in case parts are needed later, but it can be deleted.
"use client";
import React from 'react';

export function AppSidebar() {
  // console.warn("AppSidebar is deprecated and should be removed. Its functionality is now in AppHeader.");
  return null; 
}
    