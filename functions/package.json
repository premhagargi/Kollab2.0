
{
  "name": "kollab-functions-placeholder",
  "description": "Cloud Functions for Kollab (CRON job reverted to Next.js API route)",
  "scripts": {
    "build": "echo 'No build step needed for placeholder functions package' && exit 0",
    "serve": "echo 'No functions to serve locally for placeholder package' && exit 0",
    "deploy": "echo 'No functions to deploy for placeholder package' && exit 0"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}
